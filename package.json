{
  "name": "node-db4-project",
  "version": "1.0.0",
  "scripts": {
    "start": "node index.js",
    "server": "nodemon index.js",
    "latest": "npx knex migrate:latest",
    "rollback": "npx knex migrate:rollback",
    "seedmake": "npx knex seed:make",
    "seed": "npx knex seed:run",
    "reset": "npm run rollback && npm run latest && npm run seed"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/LambdaSchool/node-db4-project.git"
  },
  "dependencies": {
    "express": "^4.17.1",
    "knex": "^0.95.11",
    "sqlite3": "^5.0.2"
  },
  "devDependencies": {
    "dotenv": "^10.0.0",
    "eslint": "^7.32.0",
    "knex-cleaner": "^1.3.1",
    "nodemon": "^2.0.12"
  }
}
